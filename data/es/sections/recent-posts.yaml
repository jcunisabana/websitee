# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: false
  weight: 2
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
